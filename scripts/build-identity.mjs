import {createHash} from 'node:crypto';
import {execFileSync} from 'node:child_process';
import {readFileSync,readdirSync} from 'node:fs';
import {join,relative} from 'node:path';
export function buildIdentity(root=process.cwd()) {
 const walk=dir=>readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(join(dir,e.name)):[join(dir,e.name)]);
 const digest=files=>{const hash=createHash('sha256');for(const file of files.sort()){hash.update(relative(root,file));hash.update('\0');hash.update(readFileSync(file));hash.update('\0');}return hash.digest('hex');};
 const sourceFiles=[...walk(join(root,'src')),...walk(join(root,'scripts')),...walk(join(root,'examples')).filter(p=>!p.endsWith('-sim')),...walk(join(root,'public')).filter(p=>!relative(join(root,'public'),p).startsWith('lesson-data/')),...['package.json','package-lock.json','vite.config.ts','index.html','tsconfig.json'].map(p=>join(root,p))];
 let commit=null,dirty=true;try{commit=execFileSync('git',['rev-parse','HEAD'],{cwd:root,encoding:'utf8'}).trim();dirty=Boolean(execFileSync('git',['status','--porcelain','--untracked-files=normal'],{cwd:root,encoding:'utf8'}).trim());}catch{}
 return {schemaVersion:1,edition:JSON.parse(readFileSync(join(root,'package.json'),'utf8')).version,commit,dirty,sourceSha256:digest(sourceFiles),contentSha256:digest(walk(join(root,'src/lib/data'))),modelVersions:{investigations:'atlas-investigations-1',processes:'process-mechanisms-1',learningPrompts:'investigation-prompts-1',architecture:'architecture-execution-1',fabrication:'atlas-fabrication-flow-1',operatingHall:'1',realization:'atlas-realization-1',memory:'atlas-memory-interfaces-1',sensor:'sensor-logistic-1',retrieval:'lexical-cited-answer-1',orbital:'1',progress:'progress-evidence-2',studioRecords:'atlas-studio-record-1',matrixNetwork:'matrix-network-transfer-1',decoderProjection:'decoder-query-machine-1',ranking:'exposure-aware-ranking-1',memoryWorkload:'memory-workload-bridge-1',factory:'factory-actual-checkpoint-1',dataLineage:'data-lineage-1',evaluation:'identified-evaluation-1',applicationDecisions:'application-decisions-1',processCell:'process-cell-acceptance-1',speculativeVerification:'authored-draft-target-greedy-1',serving:'causal-kv-serving-1',learnedRetrieval:'atlas-incident-learned-1',distributedLearning:'distributed-causal-gradient-2',aiFamilies:'ai-families-1',commissioning:'commissioning-execution-1',control:'closed-loop-1',hardwareReliability:'hardware-reliability-1',foundations:'foundations-1',modelLifecycle:'causal-decoder-lifecycle-1',automation:'atlas-automation-1',machineExecution:'machine-execution-1'},builtAt:new Date().toISOString()};
}
