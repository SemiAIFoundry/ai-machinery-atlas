# Candidate validation record

Recorded 7 September 2026. Scope: the three connected investigations, selected process mechanisms, scientific/content repairs, saved-work contracts, and portable delivery. The published v1.2.0 demo is unchanged. This record is evidence of implementation checks, not a specialist approval or learner study.

## Automated checks

The coordinated `npm test` run passes **59/59 tests**, with no failures or skips. It builds TypeScript/Vite, validates **224 lessons, 62 chapters and 15 paths**, and passes **1,429 science checks** across **223 formal expressions and six prose relationships**. The curation validator checks the 224-lesson inventory, source/claim preservation, content fingerprints, review-event dates and reviewer-type boundaries.

Tests cover numerical conservation and limits, exact four-bit arithmetic, causal attention, shared scenario restoration, corrupted-case recovery, progress migration, 14 process routes and 84 states, lazy lesson assets, the readable-entry budget, and portable root/subdirectory asset references. The new GitHub workflow is configured to run the same suite; no remote workflow run is claimed before it has actually executed.

A separate read-only code review found four consequential saved-state/focus defects. All four were corrected and re-read: partial storage corruption no longer discards other valid work; external case navigation loads the correct written record; restoring saved inputs clears a stale shared snapshot; and lazy lesson navigation defers focus until its heading exists. The added regressions pass. No unresolved high-confidence blocker was identified within that limited review scope.

The entry-JavaScript budget is **350,000 gzip bytes at level 9**. The coordinated run before the last small search/provenance changes measured approximately **260 kB**, versus the approximately **584 kB** assessment baseline. The final archive report records the exact final measurement and build identity. This is entry JavaScript only, not the full transfer, a measured load time or a physical-device performance result.

## Browser observations actually performed

Tests used the Codex in-app browser on macOS with temporary CSS viewport sizes. They were performed during development and are not a claim that every workflow passed on a frozen production build.

| Workflow | Observed result |
| --- | --- |
| Oxidation deep link, 1280 × 720 | Stage 3 loads; cross-section and optional 3D inspection show the oxide/silicon interface and original surface datum, with material labels and rendered mathematics. No console error was observed during that inspection. |
| Phone lesson entry, 390 × 844 | Cold MOSFET entry renders readable content with no canvas until 3D is requested; body remains within the viewport. Science exposes rendered KaTeX and MathML. |
| Narrow investigation, 320 × 740 | Dialog settles at exactly viewport width; case navigation and learning forms remain available. This is a limited reflow observation, not a complete accessibility audit. |
| Functional stored bit | Rising edge captures D into Q; changing A changes D while Q remains held; the next edge updates Q. |
| First attempt and practice | Original prediction remains visible after a later practice save. |
| Scenario sharing | Copied URL contains only version/content/check metadata, case ID and scenario inputs; written answers are absent. Changing case updates its URL; reopening the earlier share restores its case. |
| External case change | Switching directly to a bare manufacturing case link does not carry switching answers into its empty learning fields. |
| JSON import | A complete A1 changed-load fixture imports through the file chooser, selects switching and restores 0.8 V, 20 fF, A=7 and B=5. |
| JSON export | Downloaded JSON reparses with the app validator; its 20 fF input, first-attempt text, practice text and all three version fields are preserved. Only synthetic QA text was used. |
| Investigation → uncached lesson | Opening the MAC lesson closes the dialog and focuses `lesson-heading`; no phone canvas is created. |
| Browser history | Back restores the switching investigation; Forward restores MAC and the lesson heading focus. |
| Learning path entry | The matter-to-machine path starts at silicon and advances to its next lesson, with path position and destination focus. Full detour/resume coverage remains in the review checklist. |

A later development-server restart left old preview tabs on connection-refused pages and interrupted further browser control. Existing observations remain recorded; no failed connection is counted as an application check or a passing browser test. Archive-level checks and a separate served-build handoff are recorded in the package report.

## Review package

The kit provides **nine task cards and 38 complete scenario documents**, computed expected values, source digests, specialist guidance and blank observation records. Individual fixture documents are importable; the entire fixture collection is not an app import. Synthetic timestamps and QA text are explicitly marked. The three investigators share a 21-field scenario and declared model/content/check versions.

The execution key distinguishes static placement from capacity for the next KV append. Its constrained four-die example regains static fit with a smaller context/batch, then restores a runnable step by reducing weight storage to eight bits. That is byte accounting under a fixed policy; it makes no inference about numerical accuracy.

## Remaining acceptance evidence

Physical iOS/Android tests, constrained-network measurements, complete keyboard/screen-reader and WebGL-unavailable tasks, independent domain review, and **6–9 actual learner sessions remain pending**. The user has agreed to coordinate devices and learners. Full conflicting-history and denied-storage browser exercises also remain in the checklist, although their relevant pure state contracts are tested.

Use [launch gates](launch-gates.md) to attach those actual observations. Broader rollout and later roadmap expansion retain their approved dependencies. Neither the lesson count, passing code tests nor preparation of a review kit establishes learning effectiveness or scientific completeness.
