# Learner worksheet

Build identity: __________  Date: __________  Participant code (optional): __________

For each task record: **prediction → intervention → observed trace → explanation → transfer → limit**. Keep the original prediction. Use the experience's save/export controls to preserve the full input or model state. Do not submit confidential data.

| Task | Route and experiment | Evidence to record |
| --- | --- | --- |
| D1 | `#training-data~v=1&studio=data`: keep seed 7; compare family splitting with document splitting. Normalize whitespace and inspect duplicates. | One source family, all its split memberships and one leaked pair. Explain why different row IDs do not guarantee independent evidence. |
| D2 | Same corpus: inspect 0 and 32 learned byte merges; advance the sampler once and restore its record. | Vocabulary changes, a sampled document instance, shifted target, objective mask and permitted attention keys. Explain why the separate nine-token decoder cannot silently consume this vocabulary. |
| L1 | `#tiny-causal-decoder~v=1&studio=learning`: create the default seed-7 model and complete 320 updates. | Initial/final loss, model identity and copy/swap development denominators. Trace a parameter from a forward value through gradient, clipping and optimizer state. |
| L2 | Fork the same L1 checkpoint into continuation and SFT, then preference or reward training. | Objective, target mask, trainable weights, reference policy, data mixture and forgetting on the other domain. Predict how a defective reward verifier can improve reward without improving the intended task. Leave final evaluation unopened during selection. |
| G1 | Continue L1 with distributed training. Reserve an uneven batch, finish rank 0, fail rank 1, try to commit, restart and complete rank 1. Begin reduce-scatter, submit both owner slices, then update owners and all-gather. | Example IDs, target counts, attempts, weighted gradient and optimizer counter before/after. Explain why a rank's locally averaged gradient does not carry enough denominator information. |
| S1 | Serve the committed checkpoint. Finish the bounded queue; compare cache precision or verifier settings only when their controls are available in the captured build. | Same source weights, per-request outputs, occupied/reserved cache, cancellation/release and full-precision reference. Distinguish logical iterations from measured GPU latency. |
| A1 | `#retrieval-supported-generation~v=1&studio=retrieval`: train the retrieval specimen and force the other manual. Pass its generated proposal to automation and inspect review before acting. | Model/corpus/context identities, retrieval score, task correctness, source support and authority. Explain how a supported and permitted action can still address the wrong asset. All actions affect the fictional local environment. |
| E1 | `#probability-calibration-abstention~v=1&studio=evaluation`: use the default classifier; raise minimum chosen-class probability to 1. Then insert the renamed training-family copy. | Correct/total, accepted/total, errors/accepted and overlap. Which values changed because the population changed, rather than because model weights improved? |
| P1 | `#oxide-to-cell-acceptance~v=1&studio=cell`: record default INV-01, then set fanout to 4. Separately restore and set oxide offset to +20%. | Capacitance, 50% delay, logic/noise/geometry/timing gates. Explain why load changes timing without changing the DC transfer curve in this model. |
| H1 | `#hbm-channel-generation-ras~v=1&studio=reliability`: compare one-, two- and three-bit corruptions and trusted recovery. | Detected/corrected status, decoded value, executed/discarded/retained work and the first guarantee that no longer applies. |
| F1 | `#temporal-forecast-validation~v=1&studio=decisions`: compare the default forecast with persistence, then use horizon 8. Enable the deliberate future-label leak. | Identical target rows for both methods, error denominators, prior-only fit and the zero-error invalid result. |
| C1 | In the same experience, choose code contracts. Compare identity and ordered clamp on examples, boundary cases and the finite oracle. | One counterexample with x, lower bound, upper bound, expected and actual output. What does a pass on 308 triples fail to establish? |
| R1 | `#robot-feedback-control~v=1&studio=control`: compare fitted planning with feedback, then add four delayed observations or a larger search that misses its deadline. | Estimated versus true state, applied action, constraint violations, final position/velocity and action effort. Explain why a better fitted model or larger search need not improve the closed loop. |

Transfer task: choose a route crossing at least three boundaries. State which artifact is carried unchanged, which state is transformed, and which acceptance test can fail independently. Do not claim the entire wafer-to-model system has been physically simulated.


## Joined factory and memory extensions

Choose one extension route for an additional 30–45 minutes; the existing tasks remain available. Record exact model/dependency identities and all changed settings. The factory answers below start with **Use seed 7**, not a previously trained or imported checkpoint. A carried checkpoint is a useful later transfer exercise, but its byte sizes and numerical values need a new record.

| Task | Route and experiment | Prediction and evidence to record |
| --- | --- | --- |
| J1 | `#checkpoint~v=1&studio=factory`: choose **Use seed 7**, then **Uninterrupted reference** (three updates, checkpoint cadence two, no fault). Compare rank-1 extra delay 0 s and 1 s, holding all other settings fixed. | Predict the change in job wall time and facility energy using the slowest concurrent rank and the declared 160 W active facility boundary. Record source/final weight identities, six completed rank computations, contribution bytes and the two replacement checkpoint commits. Explain why adding both rank durations would double-count overlapping work. |
| J2 | Restore **Partial write and replay**: fault during the update-2 checkpoint, cadence two, recovery enabled. Compare with J1's uninterrupted result. Also inspect **Written is not committed** before its commit marker. | Before running, count executed, rolled-back, retained and durable updates; count rank computations. At the fault, identify the last authoritative checkpoint and the incomplete replacement. After recovery, compare actual weights, moments, optimizer counter, RNG, cursor and owner shards with the uninterrupted run. State why a completed optimizer update or completed file write need not be durable. |
| J3 | Choose **Replacement does not fit** and inspect the old checkpoint and required replacement. For an exact byte-boundary extension, export a factory workspace, retain its full source checkpoint, set `rounds:2`, `fault:'none'`, `checkpointEvery:2`, `faultRound:2`, and compare `storageLimitBytes` equal to old-plus-new bytes with one byte less through validated import. | Compute the two-version storage requirement from the actual UTF-8 JSON sizes. Predict whether an insufficient store discards the old authoritative artifact, whether the two live updates remain available, and whether they are durable. Distinguish JSON bytes from tensor collective bytes, declared bytes/second from measured bandwidth, and IT energy from facility overhead. |
| M1 | `#memory-system-qualification~v=1&studio=memory-workload`: select the **two** matrix, refresh interval 0, no fault and default qualified stack. Compute C = AB by hand. Select B, word 0, nibble 0; compare `single-data` + `secded`, `double-data` + `secded`, and `double-data` + `unchecked`. | Record B[0,0]'s original and delivered bytes, the first affected read and its actual MAC operands, the published C or the event that blocks publication. Predict the changed first column if the low two data bits turn B[0,0] from 5 into 6. Explain why correction status, delivered value and independent output correctness are separate. |
| M2 | Restore the **three** matrix with no fault. Compare payload budgets 96 B and 144 B for `hbm`, `host-stage` and `read-tier-stage`. At 144 B, compare those tiers with otherwise identical settings. | Nine 32-bit values occupy 36 real bytes per matrix. Compute each 16-byte-aligned allocation, the complete A/B/C budget, B source reads and destination staging writes, and the declared staging time. Verify the same C and machine-relative command trace; show how staging shifts the joined clock. Does remote B placement eliminate its destination allocation? |
| M3 | Keep one admitted workload and change registration offset from 0 to 9.9 µm, then 10 µm, for the default 10 µm contact width. Separately restore 0.1 µm, use the three matrix, and compare persistent `triple` fault with refresh intervals 0 and 96 ns under `secded`. | Identify the exact admission gate that blocks before staging/commands. For positive overlap, determine whether this model changes timing or fault rate. For refresh, record issued commands and only the service observed before the execution horizon; compare faulty output bytes. Explain why refresh cannot repair this deliberately persistent injected fault and why a reported triple-error correction is outside the guarantee. |

For J1–J3, the readiness matrix is a separate commissioning witness, not the decoder's training arithmetic. Model updates and serialized checkpoints are computed; rank duration, link service and facility power are declared assumptions. For M1–M3, the same operand bytes enter the actual matrix consumer, but geometry admission, fault injection and command timing have separate stated assumptions. No physical device, production network, foundry qualification or learner result is established by these exercises.
