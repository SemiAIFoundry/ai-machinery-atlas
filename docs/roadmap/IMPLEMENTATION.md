# Active implementation authorization · 7 September 2026

The maintainer approved proceeding with the complete, additive roadmap and instructed that the review be assumed approved for continued implementation. All 30 requirements below remain in scope. The phase-gate language in the earlier candidate snapshot records its prior status; it no longer pauses construction. This authorization is not a claim that physical-device tests, learner sessions, specialist observations or instructor reuse occurred. GitHub release and deployment remain paused while the build is completed.

Current extension work and verification are recorded in [ADVANCEMENT.md](ADVANCEMENT.md).

---

# Implementation ledger — current pilot candidate

Prepared 7 September 2026 against the working candidate. This ledger maps **all 30 original ROADMAP.csv rows**, preserving their phases and dependency gates. It records implementation separately from scientific, accessible-task, physical-device and formative-learner acceptance. It does not authorize deployment or mark a phase-3 expansion complete.

The current candidate implements the scientific/content repairs, selected process mechanisms, three connected investigations, versioned learner-owned work, lazy delivery, reproducible identity and curation/review preparation. **Phase-2 release acceptance remains pending.** User-coordinated physical-device checks and six to nine learner sessions, together with actual domain and accessibility review, are still required. Phase-3 publication remains gated by the approved dependencies and phase-2 evidence.

## Recorded automated evidence

The earlier coordinated run passed **57 tests**. After storage recovery and next-step capacity regressions were added, the final coordinated `npm test` run passed **59/59 tests**, with **0 failures, skipped or todo tests**. Its log also records **1,429/1,429 science checks**, covering **229 cards: 223 formal math and six prose relationships**, and content validation for **224 lessons, 62 chapters and 15 paths**. These are automated results, not 59 user tasks, a specialist sign-off, a screen-reader audit or a learning study.

The final log measures entry JavaScript at **260,018 local gzip bytes (level 9)**, below the **350,000-byte** entry budget. This compares the same entry-JS measure with the approximately 584 kB baseline; it does not count every subsequently fetched lesson, optional chunk, CSS, font or film resource and is not an observed physical-device performance metric. Navigation metadata is separated from 224 content-addressed lesson assets; optional destinations are lazy loaded.

The [candidate validation record](../learning-review/CANDIDATE-VALIDATION.md) records the completed coordinated run and browser checks. A preliminary supplementary check encountered a changing build directory; its results were superseded by the completed build-and-test run. Automated source/data checks do not substitute for observed learner or specialist evidence.

The pure checks cover selected mathematical/material/state invariants and portability. The coordinating review records limited in-app browser checks at 320 CSS pixels, wrong-case draft prevention and shared-case restoration; these remain **limited browser observations**, not physical iOS/Android or assistive-technology evidence. The prepared device/learner kit starts all external checks as not run.

## Candidate identity

The final review archive includes `build-info.json`, source and static archive checksums, and the coordinated verification log. These identify the built candidate's exact source commit, source/content hashes and model/prompt versions. The development preview retains edition `1.2.0` until the successor's release decision; no old release tag is moved. See the [candidate validation record](../learning-review/CANDIDATE-VALIDATION.md) for the scope of observations and the package report for its frozen build identity.

## Row-by-row implementation and remaining acceptance

Paths in the table are relative to repository root. G0–G7 refer to the [pilot validation and launch gates](../learning-review/launch-gates.md). **Implemented candidate** means an artifact exists with the stated evidence; it does not mean its remaining gate has passed. **Gated extension** retains the original roadmap commitment and dependencies, even when useful foundation assets already exist.

### Repairs and promoted pilot

| ID / phase / original dependencies | Original deliverable | Current candidate artifact and disposition | Verification recorded | Remaining gate |
| --- | --- | --- | --- | --- |
| **R01** · phase 0<br>Depends: none | Correct three confirmed radical-rendering defects | **Implemented candidate.** Canonical equations in `equations-original.json`; `equation.tsx`; energy-axis `band-diagram.tsx` replaces the ambiguous energy-band anatomy display. | The 1,429-check science suite includes meaningful radical-subtree regressions for carrier density, contact transfer length and normalization; corrupt variants are rejected. | G1/G2: independent device/math review and displayed-reading/assistive-technology checks on the promoted build. |
| **R02** · phase 0<br>Depends: R01 | Review all 229 science cards and replace heuristic formula authoring | **Implemented corpus; review pending.** All 229 card positions have equation definitions: 223 formal math cards and six prose relationships, with symbol/unit tables; `equations-{original,expansion,infrastructure}.json` and `science-panel.tsx`. | 1,429/1,429 checks: strict KaTeX/MathML, selected grouping mutations, independently calculated examples and dimensional checks. The missing N row on the capacity card is now corrected. | G1: actual domain review of scientific meaning, regimes and assumptions. All cards have explicit classifications; automation does not certify every scientific claim. |
| **R03** · phase 0<br>Depends: none | Normalize domain IDs and review chapter placement | **Implemented candidate.** Six stable domains; all 62 chapters deliberately mapped in `domains.json` and `chapter-domain-map.json`; complete authoring corpus separated from generated runtime catalog. | Current validation: 224 assigned lessons, no missing records, 62 chapters and 15 valid journeys; curation checks stable domain/chapter IDs. | G0/G4: freeze the candidate and complete task-based navigation/access review; do not infer usability from ID coverage. |
| **R04** · phase 0<br>Depends: none | Rewrite 38 confirmed authoring directions for learners | **Implemented candidate.** `legacy-foundations.json` and `expansion-description-overrides.json` replace 39 confirmed instruction leaks: 30 originals plus nine expansions. This includes one additional `tensor-memory` leak found after the 38-item plan. | Editorial asset validation confirms the replacement-ID set and known instruction-pattern removal; descriptions preserve scientific qualifiers. | G1/G2: compare promoted descriptions with the actual scenes and supported controls. A lexical scan cannot prove universal editorial quality. |
| **R05** · phase 0<br>Depends: R03 | Complete 44 missing scaffolds and review prerequisites for 97 originals | **Implemented candidate.** All 97 original lessons have explicit prerequisite dispositions, including two roots; 44 missing objective/example/evidence-note triples filled in `legacy-foundations.json`. | Merged-content checks find all 224 scaffold triples; valid prerequisite IDs and an acyclic graph. The outside content-asset report records 53 arithmetic checks for the additions. | G1/G6: domain and learner review of conceptual dependency choices, examples and entry burden. A 22-node longest graph chain is not a compulsory study gate or mastery measure. |
| **R06** · phase 0<br>Depends: none | Map lesson-to-lab entry and correct misleading controls | **Implemented candidate.** `lesson-lab-map.json` covers 224 IDs: 74 direct calculator links and 150 explicit library dispositions; 39 engineering submode links. `Lab` receives contextual mode/route; unsupported process cases explicitly offer a sequence description. | Mapping validator checks existing lab/deep-lab/investigation IDs and scope; runtime reads `labId`/`deepLabId`. Generic signal/heat motion is no longer presented as a computed trace. | G2/G4: complete actual lesson→lab→return and failure workflows, including library and all three bonding routes, on required access paths. |
| **R07** · phase 0<br>Depends: none | Reconcile plan completion and add reproducible build identity | **Implemented candidate ledger/identity.** This 30-row ledger; `scripts/build-identity.mjs`, `dist/build-info.json`, `reading-guide.tsx` edition panel; source/content hashes and three model/prompt versions. | Delivery test verifies schema, source/content digests, model identity and edition; the final coordinated log passes. Candidate identity is carried with the archive. | G0/G7: freeze/tag the actual reviewed candidate, attach final evidence and map any live deployment to that identity. The candidate is not a published release. |
| **R08** · phase 0-2<br>Depends: R03 | Add validated data contracts and critical browser workflows | **Partial candidate; integration checks pending.** `validate-atlas.mjs`, `use-lesson.ts`, `url-state.ts`, versioned `learning-progress.ts`, strict investigation import and per-case browser recovery, dialog focus deferral and resource boundaries. | Final coordinated suite: 59/59 including progress migration, malformed-state rejection, deterministic restoration, delivery and added hydration-recovery and append-capacity checks; 224 content-addressed lesson assets. | G0/G3/G4: complete deep-link/back/forward, lazy failure, focus return, import-conflict and WebGL-unavailable workflows on a frozen candidate. Node/source checks are not complete browser QA. |
| **R09** · phase 0-2<br>Depends: none | Measure devices and reduce readable-entry payload | **Automated entry budget passed; physical delivery pending.** `atlas-authoring.ts` feeds generated navigation metadata and 224 lazy lesson assets; 3D, science, labs, investigations, comparison and global map are optional imports. Renderers use visibility/context-loss/disposal handling. | Final log: 260018 bytes local gzip level 9 for entry JavaScript, below 350,000 bytes; prior baseline was approximately 584 kB by this method. Delivery/portability tests pass. | G5: physical iOS Safari and Android Chrome, constrained networks, cold/warm cache, lifecycle/resource measurements and raw observations. This is entry JS only, not full transferred bytes, field LCP/INP/CLS or a physical-device result. |
| **R10** · phase 0-2<br>Depends: R08 | Validate complete learning tasks beyond viewport layout | **Access mechanisms implemented; audit pending.** Skip link, focusable destination heading, deferred focus after lazy lesson load, control labels, table/SVG descriptions, non-3D diagrams, reduced-motion handling and resource-error paths; complete checklist in `docs/learning-review/`. | Source inspection confirms mechanisms; the coordinating review records limited in-app browser/320 CSS-pixel checks. No complete screen-reader or physical-device evidence is claimed. | G4/G5: finish essential inspect/change/calculate/explain/save tasks with keyboard, VoiceOver, TalkBack and a recorded desktop screen-reader setup; reflow, contrast, motion and failure recovery require observed results. |
| **R11** · phase 1<br>Depends: R03, R08 | Author typed physical/process/execution relationships | **Implemented pilot graph.** `engineering-relations.json`: 75 scoped relations, 16 typed meanings and 75 connected lesson IDs; provenance names a source lesson and its cited URLs; typed edges precede generic edges. | All endpoints, source lesson IDs, relation IDs and typed triples validate; containment/assembly subset is acyclic. All three investigations receive relevant links. | G1/G2: domain review of direction and scope. The engineering graph is not the prerequisite DAG; a physical/data/heat link never implies a company supply contract. |
| **R12** · phase 1<br>Depends: R08 | Connect existing pure numerical models to shared versioned state | **Implemented three-investigation state.** `investigation-models.ts` shares versioned route, wafer inputs, stack count, facility/workload parameters, timing and held-bit/clock-edge state across the three investigation views and exports. Existing pure numerical models are reused. | Tests exercise deterministic import/restore, wafer-screening once, conditional stack/assembly survival, byte conservation, serial versus overlapping service and zero runnable rates when required constraints fail. | G1/G2/G3: review all resource boundaries and cross-view state correspondence. Standalone calculators retain their own state; this does not mean every lab has been unified. Run frozen-candidate UI restoration checks. |
| **R13** · phase 1<br>Depends: R08, R12 | Introduce a mechanism-specific scene/state registry | **Implemented selected mechanism registry.** `process-mechanisms.json`/`.ts` provides 14 routes × six states (84 states); `process-mechanism.tsx` and `process-spatial.tsx` share drawing state, quantities, material labels and source scope. Other routes explicitly remain sequence maps. | Eight process tests include oxidation boundary movement, ALD purge retention, intact target film at development, mask exhaustion, NA scaling, route distinctions and invariant inspection geometry. | G1/G2/G4: scientific and accessible representation review. This is a selected-route registry, not a mechanistic simulation for all 224 lessons; extruded depth remains illustrative. |
| **R14** · phase 1-2<br>Depends: R02, R05, R11, R12, R13 | Two wafer streams to a qualified accelerator pilot | **Manufacturing pilot implemented; qualification not claimed.** Investigation B carries separate logic/memory wafer inputs through screened dies, HBM survival, package convergence, compatibility and powered full racks. TC-NCF, MR-MUF and hybrid have distinct interface sequences. | Numerical/route tests pass; 38 review fixtures cover task stages and recovery cases. Taller stacks can fail envelopes; supply changes move to the next constraint; failed required constraints suppress usable output. | G1/G2/G6: semiconductor review and observed learners. Published product qualification, factory queueing and commissioning are not simulated; the model explicitly assumes commissioned infrastructure. |
| **R15** · phase 1-2<br>Depends: R02, R05, R12, R13 | Switching event to multiply-accumulate pilot | **Switching pilot implemented; transfer review pending.** Investigation A combines gate/current and RC charging, a common-axis timing plot, explicit rising-edge D/Q storage, exhaustive four-bit addition/carry trace and a small integer MAC. | Tests check all unsigned four-bit additions, charge/stored/supply energy, load scaling, bounded state and deterministic restoration; A-task fixtures/rubrics prepared. | G1/G2/G6: device/circuit review of coordinated representations and prediction/transfer observations. Exact arithmetic does not establish physical timing closure or a full timed transistor-level adder. |
| **R16** · phase 2<br>Depends: R02, R05, R11, R12, R13 | Request to evaluated result pilot | **Execution pilot implemented; application extension gated.** Investigation C exposes actual four-token/two-dimensional masked attention, weighted values and evaluated tied readout, then explicitly changes scale to a separate 70B synthetic memory/service/powered-rack scenario. | Tests check mask/probability/weighted-value behavior, state inventory, staging, current placement and next-KV append capacity; infeasible rates are withheld. C fixtures include failure and recovery. | G1/G2/G6: ML/compiler review and learner discrimination between toy output and resource estimate. This is neither a trained language model nor a real application quality/latency benchmark. |
| **R17** · phase 1-2<br>Depends: R08, R12 | Add predict-explain-transfer and learner-owned scenario records | **Learning-record candidate implemented.** Prediction/explanation/transfer prompts, a reasoning rubric, preserved first attempt versus practice, versioned export/import, inputs-only sharing, and per-case recovery with an exportable raw backup. | Parser, restoration and recovery regressions pass in the 59-test run; synthetic fixtures exercise nested attempts and held-bit/clock state. Source review confirms same-case drafts and wrong-case reset paths. | G3/G6: observe full browser workflows, conflicting histories, denied storage/clipboard and human interpretation of first/practice evidence. Written tasks are self-review/instructor evidence, not an automated mastery certificate. |
| **R18** · phase 2<br>Depends: R14, R15, R16, R17, R10 | Observe 6-9 sessions across intended learner groups | **Review kit prepared; sessions not run.** `docs/learning-review/`: nine task cards, participant scripts, facilitator guidance, observation template, device/access checklist, 38 synthetic fixture documents and launch gates. | Fixture generator/parser and numerical checks passed; temporary installed-layout Markdown checks passed. These are preparation checks only. | G6: user coordinates six to nine actual learners across the three intended groups; record exposure, assistance, devices, misconceptions and transfer, revise critical failures and retest. No sessions or efficacy findings are invented. |

### Gated depth and sustainable operation

| ID / phase / original dependencies | Original deliverable | Current candidate artifact and disposition | Verification recorded | Remaining gate |
| --- | --- | --- | --- | --- |
| **R19** · phase 3<br>Depends: R14, R15, R18 | Extend process-specific unit-operation and integration depth | **Phase 3 extension gated; foundation assets available.** Existing W01–W04 lessons plus 14-route process registry supply selected unit-operation examples and source/measurement scope; process-copy and review assets support later integration depth. | Selected material transformations have automated state regressions, particularly oxidation, ALD, resist/development and etch. Existing curriculum/IDs are preserved. | Retain R14/R15/R18 dependencies and phase-2 exit: choose and review deeper integrated flows, propagate variation/test evidence to device/accepted-output consequences, and validate with learners. Current selected demos do not close all W01–W04 depth. |
| **R20** · phase 3<br>Depends: R14, R18 | Extend interface/reliability and physical/logical memory depth | **Phase 3 extension gated; HBM pilot available.** W05–W06 lessons, HBM stack/bonding/placement labs and three distinct process routes support package and memory depth; existing interface/yield/thermal boundaries are explicit. | Current HBM route, stack, screening, placement and byte-accounting tests pass. The pilot keeps known-good input and later survival denominators distinct. | After R14/R18, add reviewed joint/reliability and physical-bit→logical-channel→stack mapping cases with consistent qualification assumptions. A stack budget is not complete memory-device/reliability instruction. |
| **R21** · phase 3<br>Depends: R15, R16, R18 | Compare same operation across scalar/vector/SIMT/systolic mechanisms | **Phase 3 extension gated.** Existing W07 architecture lessons and `specimen-comparison.tsx` retain scoped architectural comparisons; A/C provide exact arithmetic and a small tensor operation to reuse. | Current tests establish correctness of the pilot operation and existing model invariants; they do not execute that same operation through four architectural mechanisms. | After R15/R16/R18, implement and review scalar/vector/SIMT/systolic traces with invariant numerical output and measured/declared traffic differences. Preserve generation/precision scope; no cross-vendor performance equivalence is established. |
| **R22** · phase 3<br>Depends: R11, R15, R16, R18 | Publish one reproducible computation-to-physical-artifact case | **Phase 3 extension gated.** Seven CRG lessons, W08 design lessons, typed computational/realization links and the provenance manifest retain source attribution and realization contracts. | Claim/schema and formula checks preserve CRG framing and limits; no bounded public RTL/netlist/layout artifact flow was identified in the candidate. | After R11/R15/R16/R18, produce the reproducible computation-to-physical-artifact case with precision/payload/resource contract, actual tool artifacts and independently reviewed evidence. Conceptual CRG content does not fulfill this gate. |
| **R23** · phase 3<br>Depends: R12, R14, R16, R18 | Connect byte/current/coolant/heat and operational readiness | **Phase 3 extension gated; partial system propagation available.** B/C couple memory capacity and manufacturing limits to package/rack power and synthetic useful throughput; power/cooling/network lessons and typed paths remain available. | Current conservation, overlap, energy and compatibility tests support the declared snapshot; the model explicitly excludes commissioning dates, fault/recovery, queueing and partial inventory. | After R12/R14/R16/R18, connect byte/current/coolant/heat paths under one boundary and add commissioning/fault/recovery effects on retained work without double counting. |
| **R24** · phase 3<br>Depends: R25, R23 | Maintain dated capacity/queue/allocation cases with uncertainty | **Phase 3 current-case expansion gated.** Existing global-capacity/current-topic lessons and evidence library; curation manifest distinguishes status-sensitive cases, inherited dates, claim limitations and proposed recheck triggers. | 39 inherited structured claims preserve status/scope/date metadata; schema does not infer private queue/allocation data or verification from source links. | After R25/R23, assign actual owners and maintain dated case updates with source locators. Recheck operating/building/announced and IT/facility boundaries; do not count overlapping inventories or manufacture review dates. |
| **R25** · phase 1-4<br>Depends: R08 | Unify claim provenance and assign review ownership | **Curation foundation implemented; ownership/review pending.** `docs/curation/` inventories all 224 lessons, 271 URLs, 122 inherited ledger records and 39 claims; per-lesson scope, source references, dependency/investigation links, review roles, triggers and supersession fields. | Curation validator passes complete IDs, exact inherited dates/claims, content fingerprints and reverse source use. Freshness separates baseline/changed/due states and retains reviewer actor type. All new review dates and assigned reviewers are null. | G0/G1 and continuing operations: assign real review owners, check promoted claims at exact locators, record actual source/specialist events and exercise supersession. Proposed roles and a manifest alone do not satisfy owned evidence review. |
| **R26** · phase 3<br>Depends: R16, R18, R25 | Deepen data/optimization/evaluation and contrasting application cases | **Phase 3 extension gated.** Current W11/model lessons, C attention/readout, quality-versus-throughput distinctions, worked scaffolds and curation roles provide foundations. | Toy calculation and resource invariants pass; prediction/transfer tasks explicitly ask what output evidence would test quality. | After R16/R18/R25, deepen data/optimization/evaluation and add at least two contrasting application cases with real evaluated quality, failure, latency and resource tradeoffs. The miniature identity task is not two application studies. |
| **R27** · phase 3<br>Depends: R16, R23, R25 | Couple workload to bounded mission feasibility cases | **Phase 3 extension gated.** Existing W12 orbital lessons and individual energy/storage/radiation/link/radiator/economics models retain announced/research/forecast boundaries. | Existing pure model tests and source metadata checks cover bounded submodels; source presence is not validation of a complete orbital data-center mission. | After R16/R23/R25, connect a defined workload to a reviewed, internally consistent mission and reliability budget with sensitivity and unresolved requirements. Avoid implying system feasibility from one subsystem bound. |
| **R28** · phase 3<br>Depends: R02, R25, R26 | Link breakthroughs to mechanisms and constrained progress | **Phase 3 extension gated; history preserved.** Existing milestone timeline and lesson histories remain linked to cited mechanisms; formal scaling expressions and historical fitted-growth qualifiers are preserved. | Content/source-link validation and selected scaling-law grouping/numerical optimization checks pass. Historical rates remain scoped to their source sample and period. | After R02/R25/R26, review a breakthrough→worked mechanism→changed bottleneck sequence and evidence of capability versus installed capacity. Formula correctness does not complete the new historical learning pathway. |
| **R29** · phase 3-4<br>Depends: R18 | Refresh navigation companion around completed experiences | **Phase 3–4 refresh gated; existing film retained.** Existing locally packaged Ascent film/narration and navigation remain portable; current reading guide describes the new investigations and source/build boundaries. | Portable tests verify local film resources, authored JavaScript, base-relative navigation and license/metadata preservation. | After R18, refresh film/walkthrough around the completed reviewed pilots with faithful interactions, captions/transcript, lesson chapter links and frozen build identity. Existing-film portability is not a refreshed companion. |
| **R30** · phase 4<br>Depends: R18, R25 | Enable second-author review and instructor reuse | **Contribution/instructor preparation available; phase 4 demonstration pending.** Contributor workflow, review event template, source-freshness scripts, no-account scenario export/import and review-kit outcomes/tasks/rubrics/fixtures. | Curation and fixture validation pass; the initial event log is empty and no second-person review or actual instructor reuse is recorded. | After R18/R25, have a second contributor author and complete review of a compliant unit; observe instructor reuse and revise the pack. Agent-generated guidance is preparation, not evidence of an independent second author. |

## Next acceptance record

Use the [launch decision record](../learning-review/launch-gates.md), [device/accessibility checklist](../learning-review/device-and-accessibility-checklist.md), [learner-review kit](../learning-review/LEARNER-REVIEW-KIT.md) and [curation workflow](../curation/CURATION-WORKFLOW.md). Assign actual people before claiming review ownership. Record candidate identity, tasks, device/access method, observations, failures, fixes and focused retests. A missing device, specialist or participant is an explicit evidence gap.

The promotion decision should name the three pilots and their limits. It must retain pending W01–W12 depth, same-operation architecture comparison, reproducible RTL/netlist/layout realization, two application studies, history pathway, refreshed film and second-author/instructor demonstration. Existing lesson count, passing calculations and a prepared kit do not substitute for those outcomes.
