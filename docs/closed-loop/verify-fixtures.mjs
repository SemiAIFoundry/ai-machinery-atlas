import fs from 'node:fs';import {createHash} from 'node:crypto';
import {runClosedLoop,defaultControlInput} from '../../src/lib/closed-loop.ts';
const fixtures=Object.entries({nominal:{},feedback:{controller:'proportional'},openLoop:{controller:'open-loop'},delay:{delay:4},wrongGain:{model:'wrong-gain'},missedDeadline:{deadline:40},largerSearch:{horizon:5},changedPlant:{plantGain:.4,plantRetention:1.02}}).map(([id,patch])=>{const r=runClosedLoop({...defaultControlInput,...patch});return {id,input:r.input,metrics:r.metrics,fitted:r.fitted};});
const sources=['src/lib/closed-loop.ts','src/components/closed-loop.tsx','src/components/closed-loop.css','src/components/portable-experiment.tsx','scripts/closed-loop.test.mjs'];
fs.writeFileSync('docs/closed-loop/validation.json',JSON.stringify({date:new Date().toISOString(),actor:{type:'agent',id:'/root'},scope:'Automated mathematical and program checks; no physical or human learning observation',focusedTests:11,files:sources.map(path=>({path,sha256:createHash('sha256').update(fs.readFileSync(path)).digest('hex')})),fixtures,pending:['Independent specialist review','Physical device accessibility and interaction checks','Human learner observations']},null,2)+'\n');
